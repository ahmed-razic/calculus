#Define a function
f(x) = sqrt(x)
g(x) = sqrt(x + 0.81)

set terminal qt 0
set grid
set xtics 1
set ytics 1
set size ratio 1
set xrange [-3:3]
set yrange [-3:3]
set arrow nohead from -3,0 to 3,0
set arrow nohead from 0,-3 to 0,3
plot g(x) with lines, f(x) with lines
#replot [0 : 3] f(x) with lines

pause -1
#EOF

