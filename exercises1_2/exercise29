#Define a function
f(x) = x**3
g(x) = (x + 1)**3 - 1

set terminal qt 0
set grid
set xtics 1
set ytics 1
set size ratio 1
set xrange [-3:3]
set yrange [-3:3]
set arrow nohead from -3,0 to 3,0
set arrow nohead from 0,-3 to 0,3
plot f(x) with lines, g(x) with lines

pause -1
#EOF
